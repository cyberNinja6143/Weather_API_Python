Everything in this directory is not necessary for the API to work. 

This directory (tests directory) contains
tests made to individually test various components of this API without starting up the API and hitting each endpoint and
every endpoint in whatever order required.